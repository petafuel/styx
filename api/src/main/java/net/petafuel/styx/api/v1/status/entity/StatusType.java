package net.petafuel.styx.api.v1.status.entity;

/**
 * defines whether an internal redirect should be an error, success or any other additional status
 */
public enum StatusType {
    ERROR,
    SUCCESS
}
